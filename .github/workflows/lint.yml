name: Lint
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: astral-sh/uv@v0.4.0
    - run: uv sync --dev
    - run: uv run ruff check .
